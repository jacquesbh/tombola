{% extends 'base.html.twig' %}

{% block title %}Join the Raffle{% endblock %}

{% block body %}
<div class="min-h-screen flex items-center justify-center px-4">
    <div class="absolute top-4 right-4">
        <select id="language-selector" class="bg-white border-2 border-gray-300 rounded-lg px-3 py-2 text-sm font-semibold cursor-pointer hover:border-purple-500 focus:outline-none focus:ring-2 focus:ring-purple-500 transition-colors">
            <option value="en">🇬🇧 English</option>
            <option value="fr">🇫🇷 Français</option>
        </select>
    </div>
    
    <div class="bg-white rounded-3xl shadow-2xl p-8 max-w-md w-full">
        <div class="text-center mb-8">
            <h1 class="text-5xl mb-4">{{ app_emoji_join }}</h1>
            <h2 class="text-3xl font-bold text-gray-800 mb-2" data-i18n="form.title">Join the Raffle</h2>
            <p class="text-gray-600" data-i18n="form.subtitle">Fill in your information to participate</p>
            <p class="text-sm text-gray-500 mt-2" data-i18n="form.privacy">We don't record any data.</p>
        </div>

        {% if error is defined %}
            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded-lg mb-6">
                {{ error }}
            </div>
        {% endif %}

        <form method="POST" action="{{ path('join_submit', {code: code}) }}" class="space-y-6" id="join-form">
            <input type="hidden" id="playerId" name="playerId" value="">
            
            <div>
                <label for="firstName" class="block text-sm font-semibold text-gray-700 mb-2" data-i18n="form.firstName">
                    First Name
                </label>
                <input type="text" 
                       id="firstName" 
                       name="firstName" 
                       required
                       class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent"
                       data-i18n-placeholder="form.firstNamePlaceholder"
                       placeholder="Your first name">
            </div>

            <div>
                <label for="lastName" class="block text-sm font-semibold text-gray-700 mb-2" data-i18n="form.lastName">
                    Last Name
                </label>
                <input type="text" 
                       id="lastName" 
                       name="lastName" 
                       required
                       class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent"
                       data-i18n-placeholder="form.lastNamePlaceholder"
                       placeholder="Your last name">
            </div>

            <div>
                <label for="email" class="block text-sm font-semibold text-gray-700 mb-2" data-i18n="form.email">
                    Email
                </label>
                <input type="email" 
                       id="email" 
                       name="email" 
                       required
                       class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent"
                       data-i18n-placeholder="form.emailPlaceholder"
                       placeholder="your@email.com">
                <p class="text-xs text-gray-500 mt-1" data-i18n="form.gravatarNote">Used for your Gravatar avatar</p>
            </div>

            <button type="submit" 
                    class="w-full bg-gradient-to-r from-purple-600 to-indigo-600 hover:from-purple-700 hover:to-indigo-700 text-white font-bold py-4 px-6 rounded-lg text-lg shadow-lg transform hover:scale-105 transition-all duration-200"
                    data-i18n="form.submit">
                🎯 Participate!
            </button>
        </form>
    </div>
</div>

<script>
const code = '{{ code }}';

const translations = {
    en: {
        'form.title': 'Join the Raffle',
        'form.subtitle': 'Fill in your information to participate',
        'form.privacy': '🔒 Your data is only used during the event and not stored.',
        'form.firstName': 'First Name',
        'form.firstNamePlaceholder': 'Your first name',
        'form.lastName': 'Last Name',
        'form.lastNamePlaceholder': 'Your last name',
        'form.email': 'Email',
        'form.emailPlaceholder': 'your@email.com',
        'form.gravatarNote': 'Used for your Gravatar avatar',
        'form.submit': '🎯 Participate!'
    },
    fr: {
        'form.title': 'Rejoindre la Tombola',
        'form.subtitle': 'Remplissez vos informations pour participer',
        'form.privacy': '🔒 Vos données sont utilisées uniquement pendant l\'événement et ne sont pas conservées.',
        'form.firstName': 'Prénom',
        'form.firstNamePlaceholder': 'Votre prénom',
        'form.lastName': 'Nom',
        'form.lastNamePlaceholder': 'Votre nom',
        'form.email': 'Email',
        'form.emailPlaceholder': 'votre@email.com',
        'form.gravatarNote': 'Utilisé pour votre avatar Gravatar',
        'form.submit': '🎯 Participer !'
    }
};

function setLanguage(lang) {
    localStorage.setItem('tombola_language', lang);
    
    document.querySelectorAll('[data-i18n]').forEach(element => {
        const key = element.getAttribute('data-i18n');
        if (translations[lang] && translations[lang][key]) {
            element.textContent = translations[lang][key];
        }
    });
    
    document.querySelectorAll('[data-i18n-placeholder]').forEach(element => {
        const key = element.getAttribute('data-i18n-placeholder');
        if (translations[lang] && translations[lang][key]) {
            element.placeholder = translations[lang][key];
        }
    });
    
    document.getElementById('language-selector').value = lang;
    document.title = translations[lang]['form.title'] + ' - Tombola';
}

const savedLanguage = localStorage.getItem('tombola_language') || 'en';
setLanguage(savedLanguage);

document.getElementById('language-selector').addEventListener('change', (e) => {
    setLanguage(e.target.value);
});

const savedData = sessionStorage.getItem(`tombola_player_${code}`);
if (savedData) {
    try {
        const player = JSON.parse(savedData);
        const firstNameInput = document.getElementById('firstName');
        const lastNameInput = document.getElementById('lastName');
        const emailInput = document.getElementById('email');
        const playerIdInput = document.getElementById('playerId');
        
        firstNameInput.value = player.firstName || '';
        lastNameInput.value = player.lastName || '';
        emailInput.value = player.email || '';
        playerIdInput.value = player.playerId || '';
        
        console.log('✨ Formulaire pré-rempli avec les données de session');
        
        if (player.firstName && player.lastName && player.email) {
            console.log('🔄 Reconnexion automatique avec playerId:', player.playerId);
            setTimeout(() => {
                document.getElementById('join-form').submit();
            }, 500);
        }
    } catch (e) {
        console.error('Erreur lors du chargement des données sauvegardées:', e);
    }
}

document.getElementById('join-form').addEventListener('submit', function(e) {
    const firstName = document.getElementById('firstName').value;
    const lastName = document.getElementById('lastName').value;
    const email = document.getElementById('email').value;
    const playerId = document.getElementById('playerId').value;
    
    sessionStorage.setItem(`tombola_player_${code}`, JSON.stringify({
        playerId,
        firstName,
        lastName,
        email
    }));
    
    console.log('💾 Données sauvegardées dans sessionStorage');
});
</script>
{% endblock %}
